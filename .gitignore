# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Test / tooling
.coverage
.pytest_cache/

# Local launchd install (generated from the .example template)
com.launchddash.server.plist

# Misc
.DS_Store
*.log

# Personal app launcher config (machine-specific paths; see apps.json.example)
apps.json

# Personal job annotations (machine-specific; see labels.json.example)
labels.json

# Network-watch state (machine-specific baseline of known listeners/devices)
netwatch.json
netwatch.json.bak
netwatch.json.tmp
netwatch.log.jsonl
netwatch.json.bak-*

# The demo MP4 is a build artifact of scripts/assemble-demo.sh — the README embeds
# the GIF, nothing references the MP4, and each re-record added ~0.5 MB to history.
docs/demo.mp4
